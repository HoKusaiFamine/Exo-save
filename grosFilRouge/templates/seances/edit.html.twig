{% extends 'base.html.twig' %}

{% block title %}Modifier une séance
{% endblock %}

{% block body %}

	<div class="d-flex justify-content-center formulaire">
		<h1>Modifier une séance</h1>
	</div>

	<div class="d-flex justify-content-center formulaire">
		{{ include('seances/_form.html.twig', {'button_label': 'Update'}) }}
	</div>
	<div class="d-flex justify-content-center formulaire mt+10">
		<a class="btn btn-outline-primary mt-2" href="{{ path('app_cours_index') }}">Retour à la liste</a>
		{{ include('seances/_delete_form.html.twig') }}

	</div>

{% endblock %}
