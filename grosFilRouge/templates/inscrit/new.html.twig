{% extends 'base.html.twig' %}

{% block title %}Inscrire un élève à un cours
{% endblock %}

{% block body %}
    {# titre #}
	<div class="d-flex justify-content-center formulaire">
		<h1>Inscription à un cours</h1>
	</div>
	

<div class="d-flex justify-content-center">
		<div>
    {# formulaire #}
	{# <div class=" input-group-text-2"> #}
				{# <span class="input-group-text w-50 d-flex justify-content-center">NomNombre d'élève max.</span> #}
				<div class="  d-flex justify-content-center">{{ include('inscrit/_form.html.twig') }}</div>
			{# </div> #}
	{# <div class="mx-3">
		<div class="d-flex ">
			{{ include('inscrit/_form.html.twig') }}
		</div> #}

        {# Bouton de retour #}
		<div class="mt-5">
			<a class="btn btn-primary mt-2" href="{{ path('app_inscrit_index') }}">Retour à la liste des cours</a>
		</div>
	</div>
	</div></div>
{% endblock %}
