{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateur
{% endblock %}

{% block body %}

	<div class="d-flex justify-content-center formulaire">
		<h1>Modifier les infos d'un professeur</h1>
	</div>
	{{form_start(formEditUserAdmin)}}

	<div class="d-flex justify-content-center">
		<div>

			<div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.nom_utilisateur) }}</div>
			</div>
            	<div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.prenom_utilisateur) }}</div>
			</div>
            	<div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.email) }}</div>
			</div>
             	<div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.telephone_utilisateur) }}</div>
			</div>
            	<div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.sexe_utilisateur) }}</div>
			</div>
            <div class=" input-group-text-2">
				<div class=" d-flex justify-content-center">{{ form_row(formEditUserAdmin.roles) }}</div>
			</div>
		</div>
	</div>

			<div class="d-flex justify-content-around m-5">
				<div class=" d-flex justify-content-center">
					{{ include('utilisateur/_formEditUser.html.twig') }}
				</div>
				<div class=" d-flex justify-content-center order-1">
					<a class="btn btn-outline-primary mt-2" href="{{ path('app_utilisateur_index') }}">Retour à la liste</a>
				</div>
				{# <div class=" d-flex justify-content-center order-2">
					{{ include('utilisateur/_delete_form.html.twig') }}
				</div> #}
			</div>

	{{form_end(formEditUserAdmin)}}

{% endblock %}
