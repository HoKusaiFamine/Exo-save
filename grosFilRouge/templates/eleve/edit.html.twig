{% extends 'base.html.twig' %}

{% block title %}
	Modifier un élève
{% endblock %}

{% block body %}

	<div class="d-flex justify-content-center formulaire">
		<h1>Modifier un élève</h1>
	</div>

 <div class="d-flex justify-content-center formulaire">
	{{ include('eleve/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>

	<div class="d-flex justify-content-around m-5">
				
				<div class=" d-flex justify-content-center order-1">
					<a class="btn btn-outline-primary mt-2" href="{{ path('app_eleve_index') }}">Retour à la liste</a>
				</div>
				<div class=" d-flex justify-content-center order-2">
					{{ include('eleve/_delete_form.html.twig') }}
				</div>
			</div>
 
	{# {{ include('eleve/_delete_form.html.twig', {'button_label': 'Supprimer'}) }} #}
{% endblock %}
